========

Storage - if we don't have an SD card then simply the data isn't
saved.  The warnings about no SD card should be surpressed for the
remote board.

For the remote then we don't instantiate storage module then the
measurement's saveLastSample would be a null operation.  We would push
out to master at this point, or we simply respond to polls from the master.

NOTES:
The file rollover means the SD card is filling with no control over it
As we email the file then no need to actually save it.

=== Status OLED networking ===

Indicate that connected when waiting for NTP ?

=== SPIFFS as reference not pointer to standard SPIFFS =====

=== MONITOR webserver ===
   Fixed on core 0, and no WDT currently...

===  RECURSDION IN CONFIG AT START ====

=== UPDATE - SYNC WITH MEASUREMENT TO AVOID CORRUPTION ? ===

=== UDP DEBUGGING ===


CALIBRATION
===========

==== STATUS SCREENS =====

Remotes
   Name <DT>
   Flow Return

All
   HP  14.5 22.5 : 10.5
   UFH
   GND
   1ST
   TOP

==== POWER ESTIMATOR ? ===
Input flow rates for each floor to config
Generate mass-flow power generation for consideration ?

============

Server broadcasts to port 2222

Test.
Have 1 board sending incrementing counter to 2222
   other boards receiving counter + display

Board will broadcast to


All boards broadcast to 2222
   cjson parse - get id's
      tboards look for HP flow/return
      master processes remotes

remote sensor - new type of sensor ?

if temp-module
   add udp listener
      when packet arrives
         will have 2x sensors




could broadcast from master on 2222, therefore tboards listening on 2222
tboards bcast on 1111, master listening on 1111




