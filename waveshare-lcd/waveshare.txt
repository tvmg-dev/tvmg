From https://www.waveshare.com/wiki/ESP32-S3-Touch-LCD-4.3B the offline
package https://drive.google.com/drive/folders/1Pcs_A4FKWvdSHnz9lEBYqOpr-noTMbIv?usp=sharing
provides downloads which appear to be revisions of the arduino-esp32 git repo,
not checked against official versions and the 3.0.7 release

(my current installed arduino-esp - boards manager ? is 3.1.0)

From IDE installed (to Documents/Arduino/libraries)
Updated the ESP32_Display_Panel library to : 1.0.3
   ESP32_IO_Expander updated : 1.0.1
   esp-lib-utils : 0.1.2
   lvgl : 8.4.0

Need to set (in library for now) for using supported ws-43b board in
ESP_Panel_Board_Supported.h

   #define ESP_PANEL_USE_SUPPORTED_BOARD       (1)
   BOARD_WAVESHARE_ESP32_S3_Touch_LCD_4_3_B

Need PSRAM enabled

In the library: src/board/waveshare/ESP32_S3_Touch_LCD_4_3_B.h - this doesn't define the backlight active,
but it does in the library update.

   ESP_PANEL_BOARD_USE_LCD             1
   ESP_PANEL_BOARD_LCD_CONTROLLER      ST7262
   ESP_PANEL_BOARD_LCD_BUS_TYPE        (ESP_PANEL_BUS_TYPE_RGB)
   ...
   ESP_PANEL_BOARD_USE_TOUCH           (1)
   ESP_PANEL_BOARD_TOUCH_CONTROLLER    GT911
   ESP_PANEL_BOARD_TOUCH_BUS_TYPE      (ESP_PANEL_BUS_TYPE_I2C)
   ...
   ESP_PANEL_BOARD_USE_BACKLIGHT       (1)
   ESP_PANEL_BOARD_BACKLIGHT_TYPE      (ESP_PANEL_BACKLIGHT_TYPE_SWITCH_EXPANDER)
   ESP_PANEL_BOARD_BACKLIGHT_IO        (2)
   ESP_PANEL_BOARD_BACKLIGHT_ON_LEVEL  (1)
   ...
   ESP_PANEL_BOARD_USE_EXPANDER        (1)
   ESP_PANEL_BOARD_EXPANDER_CHIP       CH422G


Then the github source of ESP_Panel: https://github.com/esp-arduino-libs/ESP32_Display_Panel.git
   version 0.2.1
   in Documents/github/ESP32_Display_Panel
      examples/Panel/PanelTest
         modify ESP_Panel_Board_Supported.h to be same as the library
            #define ESP_PANEL_USE_SUPPORTED_BOARD       (1)
            BOARD_WAVESHARE_ESP32_S3_Touch_LCD_4_3_B
         modify ESP_Panel_Conf.h
            ESP_PANEL_ENABLE_LOG (1)      to enable logging - set Arduino core log to debug

   the paneltest example in the panel library works ok, showing colour bar & touch response.

Panel test reports a problem when debug enabled
   So the interrupt mode is disabled (possibly by defsign) but then there's
   the warning about unable to initialize i2C.  A branch exists that does some
   stuff for the 4.3 panel in that area but not 4.3B ?

   D (2457) ESP_PanelBus_I2C: Panel IO @0x3fc9eba4 created
   D (2462) ESP_PanelTouch: Disable interruption
   D (2466) GT911_CPP: Use default GT911 driver data(address: 0x5d)
   I (2472) GT911: LCD touch panel create success, version: 1.1.1
   W (2477) GT911: Unable to initialize the I2C address

   The disable interruption is ok as not set, set ESP_PanelTouch::init()

   The unable to initialize i2C message is due to not having
      esp_lcd_touch_gt911->config.rst_gpio_num &
      esp_lcd_touch_gt911->config.int_gpio_num not set when
      esp_lcd_touch_new_i2c_gt911() called

        ESP_PANEL_TOUCH_IO_RST & ESP_PANEL_TOUCH_IO_INT purposefully not set

   Doesn't seem to cause problems as such - so not a problem.

Pulled the git repo's of lvgl, ESP32_Display_Panel to Documents/github and set to same ver in libs

   /examples/arduino/board/board_static_config : ok
   /examples/arduino/gui/lvgl_v8/squareline_port : ok
   /examples/arduino/gui/lvgl_v8/simple_rotation : ok

So lvgl up & running ok.  Not sure how performant etc but ok for now.






